{% if notify %}
<li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
        <i class="ti-bell"></i>
        <p class="notification">Nuevas</p>
        <p>Notificaciones</p>
        <b class="caret"></b>
    </a>
    <ul class="dropdown-menu">

        {%  if booking %}
         <li><a href="{{ path('dash_booking') }}">Reservas sin atender</a></li>
        {% endif %}
    </ul>
</li>
{% endif %}