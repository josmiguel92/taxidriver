<div class="header">
    <h4 class="title">Editar elemento<small><i class="fa-spin fa fa-spinner hidden formLoader" style="float: right"></i></small></h4>
</div>
<div class="content">
    {{ form_start(infographitem_form) }}
    {{ form_widget(infographitem_form) }}
    <a href="#" data-toggle="modal" data-target="#iconModal" data-backdrop="false">Ver iconos disponibles</a>
    <hr>

    <div class="text-center">
        <button type="submit" class="btn btn-info btn-fill btn-wd">Guardar</button>
    </div>
    {{ form_end(infographitem_form) }}
    {% if infographitem_delete_form is not empty %}
            {{ form_start(infographitem_delete_form) }}

            {{ form_widget(infographitem_delete_form) }}
        <hr>
    <div class="text-center">
            <input type="submit" value="Eliminar" class="btn btn-danger unpressed" id="deletebtn" style="float: inherit"/>
    </div>
            {{ form_end(infographitem_delete_form) }}
        {% endif %}
    </div>
</div>




    <div class="clearfix"></div>
</div>
<script> //previene eliminar imagenes sin confirmarlo
    $("#deletebtn.unpressed").click(function(e){
        console.log('click');
        if(this.classList.contains('unpressed'))
        {
            e.preventDefault();
            this.classList.remove('unpressed');
            this.value = "¿En serio, eliminar?"
        }

    });

</script>