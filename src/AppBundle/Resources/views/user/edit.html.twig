{% extends 'AppBundle:Dash:clientNotification.html.twig' %}

{% block content %}
    <h1>Editar usuario</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Editar" class="btn btn-info"/>    <a href="{{ path('dash_security_user_index') }}" class="btn btn-primary">Regresar a la lista</a>

    {{ form_end(edit_form) }}

            {{ form_start(delete_form) }}
                <input type="submit" value="Eliminar" class="btn-danger btn" style="float: right;">
            {{ form_end(delete_form) }}

{% endblock %}
