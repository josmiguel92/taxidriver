{% extends 'AppBundle:Dash:base.html.twig' %}

{% block content %}
    <h1>Crear usuario</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Crear" class="btn-primary btn" style="float: right;"/>
    {{ form_end(form) }}


    <a href="{{ path('dash_security_user_index') }}" class="btn btn-primary">Regresar a la lista</a>
{% endblock %}
